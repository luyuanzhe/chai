diff --git a/lib/chai/assertion.js b/lib/chai/assertion.js
index 95209fb..0b00dfd 100644
--- a/lib/chai/assertion.js
+++ b/lib/chai/assertion.js
@@ -8,6 +8,7 @@
 import {config} from './config.js';
 import {AssertionError} from 'assertion-error';
 import * as util from './utils/index.js';
+import {warn} from './utils/warn.js';
 
 export class Assertion {
   /** @type {{}} */
@@ -60,7 +61,7 @@ export class Assertion {
 
   /** @returns {boolean} */
   static get includeStack() {
-    console.warn(
+    warn(
       'Assertion.includeStack is deprecated, use chai.config.includeStack instead.'
     );
     return config.includeStack;
@@ -68,7 +69,7 @@ export class Assertion {
 
   /** @param {boolean} value */
   static set includeStack(value) {
-    console.warn(
+    warn(
       'Assertion.includeStack is deprecated, use chai.config.includeStack instead.'
     );
     config.includeStack = value;
@@ -76,7 +77,7 @@ export class Assertion {
 
   /** @returns {boolean} */
   static get showDiff() {
-    console.warn(
+    warn(
       'Assertion.showDiff is deprecated, use chai.config.showDiff instead.'
     );
     return config.showDiff;
@@ -84,7 +85,7 @@ export class Assertion {
 
   /** @param {boolean} value */
   static set showDiff(value) {
-    console.warn(
+    warn(
       'Assertion.showDiff is deprecated, use chai.config.showDiff instead.'
     );
     config.showDiff = value;
diff --git a/lib/chai/config.js b/lib/chai/config.js
index 07a1bbe..40dba46 100644
--- a/lib/chai/config.js
+++ b/lib/chai/config.js
@@ -108,5 +108,24 @@ export const config = {
    * @param {Function}
    * @public
    */
-  deepEqual: null
+  deepEqual: null,
+
+  /**
+   * ### config.enableWarning
+   *
+   * User configurable property, controls whether Chai will output console warnings
+   * when deprecated assertion APIs or legacy syntaxes are used.
+   *
+   * When set to `true` (the default), using deprecated assertion syntaxes will trigger
+   * friendly warnings in the console to help guide developers toward the recommended APIs.
+   *
+   * Set it to `false` to suppress these warnings (for example, in production test runs
+   * or when the noise is not desired).
+   *
+   *     chai.config.enableWarning = false;  // disable deprecation warnings
+   *
+   * @param {boolean}
+   * @public
+   */
+  enableWarning: true
 };
diff --git a/lib/chai/utils/index.js b/lib/chai/utils/index.js
index 8dafa68..d71b48e 100644
--- a/lib/chai/utils/index.js
+++ b/lib/chai/utils/index.js
@@ -99,6 +99,9 @@ export {getOperator} from './getOperator.js';
 // events EventTarget
 export {events} from './events.js';
 
+// warn utility
+export {warn} from './warn.js';
+
 /**
  * Determines if an object is a `RegExp`
  * This is used since `instanceof` will not work in virtual contexts
